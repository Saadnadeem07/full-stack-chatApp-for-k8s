kind: Service
apiVersion: v1
metadata:
  name: backend #name of the Service. - Pods can access backend using this name http://backend
  namespace: chat-app
spec:
  selector:
    app:  backend #Send traffic to Pods having label app=backend
  type:  ClusterIP
  ports:
    port:  5001
    targetPort:  5001
